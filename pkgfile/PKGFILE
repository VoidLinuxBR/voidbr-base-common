# Maintainer: Vilmar Catafesta <vcatafesta@gmail.com>

pkgname=voidbr-base-common
pkgdesc="VoidBR TTY Meta Package"
pkgver=$(date +%Y%m%d)
pkgrel=$(date +%H%M)
provides=()  # "eu SOU o $pkgname pkg1, pra quem depender deles"
replaces=()  # "remove o pkg1 pkg2 antigo automaticamente quando eu instalar"
conflicts=() # "nunca me deixe coexistir com o pkg1 pkg2 original"
arch=('any')
license=('MIT')
#url="ssh://git@github.com/voidlinuxbr/${pkgname}"
url="https://github.com/voidlinuxbr/${pkgname}"
source=("git+${url}.git")
md5sums=('SKIP')
makedepends=()
depends=(
  bat
	7zip                    # Compactador 7z (CLI)
	acpi                    # Info de bateria/térmica (CLI)
	acpid                   # Daemon ACPI
	alsa-pipewire           # ALSA sobre PipeWire
	alsa-plugins-pulseaudio # Plugin ALSA/PulseAudio
	alsa-utils              # Utilitários ALSA
	bash-completion
	bc
	binutils
	brightnessctl           # Controle de brilho (CLI)
	chili-clonedisk
	chili-utils
	chrony                  # Versatile implementation of the Network Time Protocol (NTP)
	cpupower                # Gerenciamento de CPU (CLI)
	cryptsetup
	curl
	dbus
	dbus-elogind # Message bus system (variante elogind)
	dhcpcd
	dialog
	dracut
	duf
	efivar
	elogind # Gerenciador de login e sessões
	eza
	fastfetch
	ffmpeg     # Manipulação de áudio/vídeo (CLI)
	fuse-exfat # Suporte a exFAT (driver)
	fzf
	gettext
	gnome-keyring # Gerenciador de senhas (daemon)
	grep
	grub-btrfs-runit
	grub-i386-efi
	grub-terminus
	grub-utils
	gst-plugins-bad1  # Codecs GStreamer
	gst-plugins-base1 # Codecs GStreamer (base)
	gst-plugins-good1 # Codecs GStreamer
	gst-plugins-ugly1 # Codecs GStreamer (restrito)
	gvfs              # Sistema de arquivos virtual (daemon)
	gvfs-cdda         # gvfs para CDs de áudio
	gvfs-smb          # gvfs para SMB/Windows
	htop              # Monitor de processos (TUI)
	btop
	inetutils         # Utilitários de rede (CLI)
	intel-ucode
	iptables
	iputils # Utilitários de rede/ping (CLI)
	iw      # Configuração de dispositivos wireless (CLI)
	iwd
	jfsutils
	jq               # Processador JSON (CLI)
	libjack-pipewire # JACK sobre PipeWire
	libspa-bluetooth # Bluetooth para PipeWire
	linux-firmware
	linux-firmware-amd
	linux-firmware-intel
	linux-firmware-network
	lm_sensors # Sensores de temperatura (CLI)
	lua55      # Linguagem de script (runtime)
	lvm2
	nano
	NetworkManager # Gerenciador de rede (daemon)
	nvme-cli
	optipng         # Compressor PNG (CLI)
	p7zip           # Suporte extra a compactados (CLI)
	parallel
	parted
	pasystray
	pavucontrol
	pigz
  rtkit            # Realtime Policy and Watchdog Daemon - Prioridade de audio em tempo real (daemon)
	pipewire         # Servidor de áudio (daemon)
	playerctl        # Controle MPRIS de mídia (CLI)
	polkit-elogind   # Daemon de autorização de privilégios
	psmisc           # Utilitários de processo (CLI)
	pulseaudio       # Servidor de áudio (daemon)
	pulseaudio-utils # Utilitários CLI PulseAudio
	pv
	rsync
	sassc            # Compilador SASS (CLI)
	sed
	socklog
	socklog-void
	tree
	voidbr-base-config # Configuração base (arquivos)
	voidbr-grub-theme
	voidbr-nano-config # Configuração do nano (arquivos)
	voidbr-utils       # Utilitários gerais VoidLinuxBR (CLI)
	voidbr-vinstall
	voidbr-webapps
	wget
	wifi-firmware
	wireless_tools # Ferramentas legadas de wireless (CLI)
	wireplumber    # Sessão PipeWire (daemon)
	wpa_supplicant # Autenticação WPA/WPA2 (daemon)
	xfsprogs
	xz
	zstd
)
backup=()

package() {
	# Verify default folder
	if [ -d "${srcdir}/${pkgname}/${pkgname}" ]; then
		InternalDir="${srcdir}/${pkgname}/${pkgname}"
	elif [ -d "${srcdir}/${pkgname}" ]; then
		InternalDir="${srcdir}/${pkgname}"
	else
		#local
		InternalDir="${WORKDIR}"
	fi

	cd "${InternalDir}"
	# Copiar a estrutura de /etc, /usr e /opt para /
	local dirs=("boot" "etc" "opt" "usr" "var")
	for dir in "${dirs[@]}"; do
		if [ -d "${InternalDir}/${dir}" ]; then
			cp -a "${InternalDir}/${dir}" "${pkgdir}/"
		fi
	done

	# Install license file if present
	if [ -f "LICENSE" ]; then
		install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	fi

	# Install documentation if present
	if [ -f "README.md" ]; then
		install -Dm644 README.md "${pkgdir}/usr/share/doc/${pkgname}/README.md"
	fi
}

pre_install() {
	log "Iniciando instalação"
}

post_install() {
	log "Ativando serviços"
  mkdir -p /var/service/
	vservice add dbus --quiet
	vservice add rtkit --quiet
	vservice add socklog-unix --quiet
	vservice add chronyd --quiet
	log "Finalizando instalação"
}

pre_upgrade() {
	log "Iniciando upgrade"
}

post_upgrade() {
	log "Finalizando upgrade"
}
